#include <iostream> 
using namespace std; 
int main() 
{ 
    int t, a, x, y; 
    cout<<"1st speed: "; 
    cin>>x; 
    cout<<"2nd speed: "; 
    cin>>a; 
    cout<<"distance: "; 
    cin>>y; 
    cout<<"time: "; 
    cin>>t; 
    cout<< "distance in "<<t<<" hours = "<<x*t+a*t+y<<endl; 
} 
