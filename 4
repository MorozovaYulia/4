#include <iostream> 
using namespace std; 
int main() 
{ 
    int t, a, x, y; 
    cout<<"1st speed: "; 
    cin>>x; 
    cout<<"2nd speed: "; 
    cin>>y; 
    cout<<"distance: "; 
    cin>>a; 
    cout<<"time: "; 
    cin>>t; 
    cout<< "distance in "<<t<<" hours = "<<x*t+y*t+a; 
} 
